#include "mms.h"

SweepLocation mmsAgentTest2(Minefield &minefield, bool shouldReset)
{
	/*static int x = -1, y = 0;
	
	x++;
	if (x == minefield.getSize() - 1)
	{
		x = 0;
		y++;
	}
	
	SweepLocation click(x, y);*/
	
	SweepLocation click(0, 0);
	
	return click;
}
